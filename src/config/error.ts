export const enum UserError {
     User_Not_Found = "User not found",
     User_Already_Exist = "User already exist",
     Wrong_Credential = "wrong credential",
     Not_Enough_Money = "Balance is insufficient",
}

export const enum ServerError {
     Server_Error = "Internal server error",
}

export const enum ProductError {
     Products_Not_Found = "User not found",
     Products_Out_Of_Stock = "Product out of stock",
}
